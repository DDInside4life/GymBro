<Page x:Class="GymBro.App.Pages.ProgramsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GymBro.App.ViewModels"
      xmlns:converters="clr-namespace:GymBro.App.Converters"
      Title="Программы">
    <Page.DataContext>
        <vm:ProgramsPageViewModel />
    </Page.DataContext>

    <Page.Resources>
        <converters:DifficultyConverter x:Key="DifficultyConverter"/>
    </Page.Resources>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Программы тренировок" Style="{StaticResource Heading1Style}"/>

                <!-- Индикатор загрузки -->
                <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Загрузка..." HorizontalAlignment="Center" FontSize="16"/>
                </Border>

                <!-- Master-Slave -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Левая колонка (Master): пользователи -->
                    <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="👥 Пользователи" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Foreground="#2C3E50" 
                                       Margin="0,0,0,15"/>

                            <ListBox Grid.Row="1" 
                                     ItemsSource="{Binding Users}" 
                                     SelectedItem="{Binding SelectedUser}"
                                     Style="{StaticResource CardListBoxStyle}"
                                     ItemContainerStyle="{StaticResource CardListItemStyle}"
                                     Height="300">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding TrainingGoal}" FontSize="12" Foreground="#7F8C8D"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Grid.Row="2" 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Right" 
                                        Margin="0,15,0,0">
                                <Button Content="➕ Добавить" 
                                        Command="{Binding AddUserCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Padding="15,8"/>
                                <Button Content="✏️ Редактировать" 
                                        Command="{Binding EditUserCommand}" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Padding="15,8" Margin="10,0,0,0"/>
                                <Button Content="➖ Удалить" 
                                        Command="{Binding DeleteUserCommand}" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Padding="15,8" Margin="10,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Правая колонка (Slave): программы -->
                    <Border Grid.Column="2" Background="White" CornerRadius="15" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="📋 Программы" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Foreground="#2C3E50" 
                                       Margin="0,0,0,15">
                                <Run Text="{Binding SelectedUser.Name, FallbackValue=''}" 
                                     Foreground="#3498DB"/>
                            </TextBlock>

                            <ListBox Grid.Row="1" 
                                     ItemsSource="{Binding Programs}" 
                                     SelectedItem="{Binding SelectedProgram}"
                                     Style="{StaticResource CardListBoxStyle}"
                                     ItemContainerStyle="{StaticResource CardListItemStyle}"
                                     Height="300">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}" 
                                                           FontSize="16" 
                                                           FontWeight="SemiBold"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                    <TextBlock Text="{Binding ProgramType}" 
                                                               FontSize="12" 
                                                               Foreground="#7F8C8D"
                                                               Margin="0,0,10,0"/>
                                                    <TextBlock Text="{Binding DurationWeeks, StringFormat='{}{0} нед.'}" 
                                                               FontSize="12" 
                                                               Foreground="#7F8C8D"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding Difficulty, Converter={StaticResource DifficultyConverter}}" 
                                                       FontSize="14" 
                                                       Foreground="Orange"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <StackPanel Grid.Row="2" 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Right" 
                                        Margin="0,15,0,0">
                                <Button Content="➕ Добавить программу" 
                                        Command="{Binding AddProgramCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Padding="15,8"/>
                                <Button Content="✏️ Редактировать" 
                                        Command="{Binding EditProgramCommand}" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Padding="15,8" Margin="10,0,0,0"/>
                                <Button Content="➖ Удалить" 
                                        Command="{Binding DeleteProgramCommand}" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Padding="15,8" Margin="10,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>