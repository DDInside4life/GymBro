<Page x:Class="GymBro.App.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GymBro.App.ViewModels"
      Title="Настройки">
    <Page.DataContext>
        <vm:SettingsPageViewModel />
    </Page.DataContext>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Настройки приложения" Style="{StaticResource Heading1Style}"/>

                <Border Background="White" CornerRadius="15" Padding="30">
                    <StackPanel>
                        <TextBlock Text="Язык интерфейса" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ComboBox ItemsSource="{Binding AvailableLanguages}" 
                                  SelectedItem="{Binding SelectedLanguage}" 
                                  Width="200" Margin="0,0,0,20"/>
                        <Button Content="Сохранить" Command="{Binding SaveCommand}" 
                                Style="{StaticResource PrimaryButtonStyle}" Width="150"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>