<Page x:Class="GymBro.App.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GymBro.App.ViewModels"
      Title="Настройки">
    <Page.DataContext>
        <vm:SettingsPageViewModel />
    </Page.DataContext>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Настройки приложения" Style="{StaticResource Heading1Style}"/>

                <Border Background="White" CornerRadius="15" Padding="30">
                    <StackPanel>
                        <TextBlock Text="⚙️ Настройки (в разработке)" 
                                   FontSize="18" 
                                   Foreground="#7F8C8D" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,20"/>
                        <TextBlock Text="Здесь будут настройки профиля, уведомлений и внешнего вида." 
                                   FontSize="14" 
                                   Foreground="#95A5A6" 
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>